// Defines what a category looks like - used to group products together
export interface Category {
  id: number;   // Unique number to identify this category
  name: string; // Category name that users see (like "Electronics", "Food", etc.)
}